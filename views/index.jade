extends layout

block content
  h1 #{data.title}
  h2 #{data.venue}
  p Redirecting to #{schedUrl}
  - if (oembed)
    a(href=oembed) Check oembed
  script.
    setTimeout(function() {document.location.href="#{schedUrl}"}, 5000);
